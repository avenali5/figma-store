import React from "react";
import { ProductInfoStyle } from "./ProductInfo.style";

type Props = {
  product: any;
};

const ProductInfo = ({ product }: Props) => {
  return <ProductInfoStyle></ProductInfoStyle>;
};

export default ProductInfo;
